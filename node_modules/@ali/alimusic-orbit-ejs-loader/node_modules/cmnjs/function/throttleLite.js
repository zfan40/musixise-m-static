// simple throttle
module.exports = function throttleLitePre(fn, delay) {
  var wait = false;
  return function throttleLite() {
    if (!wait) {
      fn.apply(this, arguments);
      wait = true;
      setTimeout(function throttleLiteTimeOut() {
        wait = false;
      }, delay);
    }
  };
};
