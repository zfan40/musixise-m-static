//var ownProperty = require('../object/ownProperty');
//
//module.exports = function (arr) {
//  var
//    i,
//    c,
//    u = {},
//    a = [];
//  for (i = 0,
//         c = arr.length; i < c; i++) {
//    if (ownProperty.call(u, arr[i])) {
//      continue;
//    }
//    a.push(arr[i]);
//    u[arr[i]] = 1;
//  }
//  return a;
//};

module.exports = function arrayUnique(arr) {
  return arr.filter(function (value, index, arrRef) {
    return arrRef.indexOf(value) === index;
  });
};
