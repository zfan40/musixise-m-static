module.exports = function returnMethod(el, method) {
  if (el[method]) {
    return el[method].bind(el);
  } else {
    return function () {
      throw new Error(method + ' not supported on this DOM element');
    };
  }
};
