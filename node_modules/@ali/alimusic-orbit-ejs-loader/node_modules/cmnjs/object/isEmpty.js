var ownProperty = require('./ownProperty');

module.exports = function objectIsEmpty(obj) {
  for (var name in obj) {
    if (ownProperty.call(obj, name)) {
      return false;
    }
  }
  return true;
};
