// key-value pair replacement

module.exports = function replaceRegExp(str, regexp, charObj) {

  if (!str || !charObj) {
    return str;
  }

  str = str.replace(regexp, function (match) {
    var returnChar = charObj[match];
    return returnChar ? returnChar : match;
  });

  return str;

};
