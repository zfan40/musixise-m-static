// ponyfill

module.exports = (function repeatString() {
  if (String.prototype.repeat) {
    return function (str, times) {
      return typeof times === 'number' && String(str).repeat(Math.abs(times));
    };
  } else {
    return function (str, times) {
      return typeof times === 'number' && new Array(Math.abs(times) + 1).join(str);
    };
  }
}());
