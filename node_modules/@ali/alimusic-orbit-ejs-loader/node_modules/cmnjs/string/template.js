var ownProperty = require('../object/ownProperty');

module.exports = function template(str, sub) {
  for (var key in sub) {
    if (ownProperty.call(sub, key)) {
      str = str.replace(new RegExp('{' + key + '}', 'g'), sub[key]);
    }
  }
  return str;
};
