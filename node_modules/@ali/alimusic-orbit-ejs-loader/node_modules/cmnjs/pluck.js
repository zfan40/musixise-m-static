// var getLength = pluck('length');
// getLength('SF Giants are going to the World Series!'); //40

module.exports = function pluckPre(name) {
  return function pluck(object) {
    return object[name];
  };
};
