var typeCheck = require('./check');

module.exports = function typeIs(obj, typeToTest) {
  var type = typeCheck(obj);
  return typeCheck(typeToTest) === 'regexp'
    ? typeToTest.test(type) : type === typeToTest;
};
