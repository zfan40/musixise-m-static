//true for: true, 'true', 'yes', 1, '1' or your own regex

module.exports = function checkBoolean(string, regexp) {
  regexp = regexp || /^true|yes|1$/i;
  return regexp.test(String(string).trim());
};
